// math.test.ts
import { getCurrentCalendar, appToCalendar } from './index';

describe('Calendar Functions', () => {
  test('getCurrentCalendar', () => {
    expect(getCurrentCalendar()).toBe([]);
  });

  test('subtract', () => {
    expect(true).toBe(true);
  });
});